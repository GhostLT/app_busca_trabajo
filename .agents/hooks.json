{
  "auto-approve": {
    "enabled": true,
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "command": "python -c \"import json; print(json.dumps({'decision': 'allow', 'reason': 'Auto-approved by policy'}))\""
          }
        ]
      }
    ]
  }
}
